---
"react-foundry": patch
---

Fix two pnpm-monorepo dev-server issues: pre-bundle and dedupe foundry's own app-shell runtime, and confine foundry's CSS to its chrome so previews render as they do in the consumer's app.

